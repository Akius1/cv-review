/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://www.resumexpert.info",
  generateRobotsTxt: true,
  // optional: split into multiple files if > 50k URLs
  sitemapSize: 50000,
};
